/**
Générer des numéros aléatoires qui sont uniques et consécutifs.

@returns Une fonction qui, lorsqu'elle est appelée, renvoie un nombre aléatoire qui n'est jamais le même que le précédent.

@example
```
import uniqueRandom = require('unique-random');

const random = uniqueRandom(1, 10);

console.log(random(), random(), random());
//=> 5 2 6
```
*/
declare function uniqueRandom(
	minimum: number,
	maximum: number
): () => number;

export = uniqueRandom;
